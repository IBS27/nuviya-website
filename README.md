# Nuviya Landing Page

Marketing site for Nuviya — the agentic email client.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies: `npm install`
2. Run the app: `npm run dev`
3. Open http://localhost:3000
